'use strict';

// Preserve the existing command; exercise the current launcher, account isolation,
// independent settings, PNG exports, dialogs and every page at both sizes.
require('./verify_launcher_redesign');
